version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16.x
    commands:
      - cd client
      - npm ci
  build:
    commands:
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: client/build
cache:
  paths:
    - 'client/node_modules/**/*'